#include <stdio.h>
#include <iostream>
#include <string>

using namespace std;

void data_recovery(char const *dictionary, int *order_list) {
  for (size_t i = 0; i < order_list.length(); i++) {
    int
  }
}

int main(int argc, char const *argv[]) {
  char *word_lists;
  int *print_order;
  data_recovery(word_lists, print_order);
  return 0;
}
